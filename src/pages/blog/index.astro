---
import BlogIndex from '../_views/BlogIndex.astro';
import { getPosts, getTags } from '~/lib/posts';

const posts = await getPosts();
const tags = await getTags();
---

<BlogIndex lang="en" posts={posts} tags={tags} />
